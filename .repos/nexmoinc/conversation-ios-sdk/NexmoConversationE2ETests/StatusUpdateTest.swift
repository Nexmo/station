//
//  StatusUpdateTest.swift
//  NexmoConversation
//
//  Created by Shams Ahmed on 14/12/2016.
//  Copyright © 2016 Nexmo. All rights reserved.
//

import Foundation
import Quick
import Nimble
import NexmoConversation

class StatusUpdateTest: QuickSpec {
    
    let client = ConversationClient.instance
    
    // MARK:
    // MARK: Test
    
    override func spec() {
//        a user can mark any event (text or image) as seen after receiving it from another user
//        a user can mark any event (text or image) as seen after retrieving it via conv:events
//        a user can mark any event (text or image) as delivered after receiving it from another user
//        a user can mark any event (text or image) as delivered after retrieving it via conv:events

        afterEach {
            
        }
        
        it("") {
            
        }
    }
}
