---
title: Kotlin
language: kotlin
---

```kotlin
// Unmute member
callMember.mute(false, muteListener)

// Unmute my member
call?.myCallMember?.mute(false, muteListener)

// Unmute whole call
call?.mute(false)
```
