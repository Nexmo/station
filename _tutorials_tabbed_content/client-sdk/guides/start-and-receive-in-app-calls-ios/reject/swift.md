---
title: Swift
language: swift
menu_weight: 1
---

```swift
call.reject { error in
    if let error = error {
        // Handle reject call failure
    } else {
        // Handle reject call success
    }
}

```
